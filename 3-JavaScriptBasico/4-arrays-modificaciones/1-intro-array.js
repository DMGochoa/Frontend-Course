

// Podemos crearlo con la palabra reservada Array
const fruits = Array('apple', 'banana', 'orange', 'kiwi', 'melon', 'watermelon');
console.log(fruits);

const arrayConPosicionesVacias = Array(5);
console.log(arrayConPosicionesVacias);

const arrayDeNumeros = Array(1, 2, 3, 4, 5);
console.log(arrayDeNumeros);

// Sintaxis litral de arrays

const fruits2 = ['apple', 'banana', 'orange', 'kiwi', 'melon', 'watermelon'];
const emptyArray = [];
console.log(emptyArray);
console.log(fruits2);

// Los arrays pueden guardar cualquier tipo de dato.
