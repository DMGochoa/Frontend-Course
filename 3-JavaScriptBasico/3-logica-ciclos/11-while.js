let contador = 0;

while (contador < 10) { //mientras contador sea menor a 10 entonces
    console.log(contador); //imprime en la consola el valor de contador en ese momento
    contador++; //luego sse suma 1. ESTO ES IMPORTANTE porque sin una forma para parar el ciclo contador se volveria un ciclo infinito.
}