/**

For of es una forma de iterar sobre un objeto iterable (como un array, un string, un objeto tipo array, etc.).

for (variable of iterable) {
    CODIGO A EJECUTAR
}

*/

let list = [1, 2, 3, 4, 5, 'Pepito Perez'];

for (let item of list) {
    console.log(item);
}